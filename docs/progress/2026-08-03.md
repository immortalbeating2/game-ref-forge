# 2026-08-03

## 新 OpenAI 账号恢复与部署

### 做了什么

- 验证当前 OpenAI 账号无法访问旧 Sites project ID，并确认新账号下没有可复用项目。
- 在隔离分支 `codex/account-migration-new-openai` 创建新 Sites 项目，将 `.openai/hosting.json` 切换到新 project ID，保持 D1 binding `DB` 与 `r2: null`。
- 完成依赖安装、完整测试、typecheck、lint、生产 build、Sites source 推送、官方 helper 打包、version 1 保存和 owner-only 私有部署。
- 通过新站认证 API 先读取空库、再预览 Backup v1、提交正式恢复，最后重新导出回读并检查 Worker 错误日志与访问策略。

### 为什么做

- 用户已切换到另一个 OpenAI 账号，需要把代码、D1 schema、私有部署与 Backup v1 恢复到新账号，不能复用旧账号不可访问的 project ID。
- 本次源站业务库为空，但仍必须执行真实预览与恢复，以证明新 D1、migration、Backup v1 和认证边界完整可用。

### 影响范围

- `.openai/hosting.json` 从旧账号 project ID 切换到新项目 `appgprj_6a6f651ceaf08191812f3b7d0ed39cea`。
- 不修改运行时代码、API、领域模型、Backup v1 合同、依赖版本、来源策略或旧账号站点；旧站继续保留为短期回滚入口。
- 新站仅允许新账号所有者访问；没有增加共享用户、群组、外部访客、公开访问或自定义域名。

### 验证结果

- 隔离基线：44 个测试文件 / 463 项通过，typecheck、lint、build 通过。
- 构建包：包含 `dist/server/index.js`、`dist/.openai/hosting.json` 和三份 SQL migration；本地归档检查为 73 entries。
- Sites source：远端 `main` 精确为 `be442557c25a8e6482d099b6a47ae7fd5e6fc64f`。
- 部署：version 1 `appgprj_6a6f651ceaf08191812f3b7d0ed39cea~appgver_77a1a83069bc8191bee8392ab4338184`，deployment `appgdep_6a6fc483204081919ca703d8e04d559c`，状态 `succeeded`。
- 新生产入口：`https://game-ref-forge.ping819376526729888.chatgpt.site`。
- 恢复前 `/api/backup` 为 `ref-forge-backup` / schema `1` / `0/0/0`，证明 D1 与 migration 可用。
- 恢复预览 create / overwrite / preserve 均为 `0`，备份摘要 `48ab991c3732001bc44ac7162f60a895aff0d4a153117a67573c742e8b0d5338` 与本地 SHA-256 一致；正式恢复 HTTP 200 且 `restored: true`。
- 恢复后再导出为 schema `1`、references / syntheses / relations `0/0/0`、preferences 不包含；最近 30 分钟 Worker errors 为 `[]`。
- 访问策略为 `custom` owner-only，额外用户 / 外部访客 / 群组为 `0/0/0`；自定义域名为 `[]`。

### 网络诊断

- Sites source 初次推送与 HTTP/1.1 重试均在本地命令等待阶段超时且无输出。
- DNS、TCP 443、TLS 和 Git receive-pack advertisement 均正常；后续 `git push --dry-run` 报告远端 up to date，`ls-remote` 精确回读目标提交。
- 结论：提交已被服务端接收，但本地命令等待结束响应时先触发超时；未再次推送或重建项目、版本。

### 风险与下一步

- `npm install` 报告依赖树存在 19 项审计告警；本次未执行 `npm audit fix --force`，避免在账号迁移中引入未经设计的破坏性依赖升级。后续应独立做安全审计与兼容性修复。
- 本次备份为空业务库，因此恢复证明的是格式、D1 和流程完整性；后续新增真实参考后应再次导出并异地保留 Backup v1。
- 新项目尚未配置自定义域名。需要域名时，先在新 Sites 项目添加 hostname，再按平台返回的实际 CNAME/验证记录配置 DNS。
