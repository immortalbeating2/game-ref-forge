# 2026-07-31

## Round 14 密度控件语义修复

- 分支：`codex/fix-density-control-semantics`
- 模式：部署后小型视觉回归修复 / TDD
- 为什么做：舒适密度使用 `Rows3` 图标，视觉上暗示横向列表，但实际合同是紧凑/舒适两档网格密度；同时两档主要只体现为宽度差异，反馈不够完整。

## 完成内容

- 先在 `tests/reference-toolbar.test.tsx` 增加图标语义回归，要求紧凑/舒适分别使用小网格和大网格。
- 在 `tests/visual-assets.test.ts` 增加次要标签显隐及移动端舒适回退回归。
- 将紧凑图标改为 `Grid3X3`，舒适图标改为 `Grid2X2`，移除误导性的 `Rows3`。
- 紧凑模式隐藏卡片底部次要标签预览；舒适模式完整显示。
- `<=820px` 继续隐藏密度控件并采用舒适触控布局，同时恢复标签预览。

## 影响范围

- 只修改 reference toolbar 图标、reference card 密度 CSS 和对应测试。
- 不新增横向列表视图，不改变密度偏好存储格式、筛选结果、选择状态或比较状态。
- 不改变 API、D1、migration、Backup v1、依赖、reference/synthesis 模型或业务数据。

## 验证结果

- TDD 红灯：图标类与密度标签显隐测试在修复前按预期失败。
- focused green：2 个测试文件 / 6 项测试通过。
- 完整门禁：39 个测试文件 / 422 项测试、typecheck、lint、build、diff check 全部通过。
- 本地浏览器桌面：
  - 紧凑：选中 `Grid3X3`，卡宽约 `241px`，两条次要标签预览均隐藏，横向溢出 `0`。
  - 舒适：选中 `Grid2X2`，卡宽约 `322px`，两条次要标签预览均显示，横向溢出 `0`。
- 本地浏览器 `390x844`：密度控件隐藏，标签预览 2，两个置顶目标均为 `44x44px`，横向溢出 `0`。
- 两条临时 `QA Density` reference 已删除，残留 `0`。

## 风险与遗留

- 本次不实现真正的横向列表；若后续需要，应作为独立“列表视图”设计，不复用密度语义。
- 当前为本地分支验证证据；合并、部署和生产只读复测将在同一流程内补录。

## 下一步

- 提交并 fast-forward 合并到 `main`，复跑 merged-main 门禁。
- 推送精确运行时提交、部署 Sites 新版本，并在生产站只读验证图标、显隐、刷新持久化和无溢出。

## Delegation Log

- 本次小型单点修复由主代理单线完成，未启用 subagent。
