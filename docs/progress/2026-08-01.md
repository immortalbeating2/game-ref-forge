# 2026-08-01

## Round 15 受保护的高保真工作台重建设计

- 分支：`codex/round-15-protected-a`
- 模式：大功能设计阶段 / Superpowers brainstorming + Impeccable
- 稳定生产基线：Sites version 18 / `4ec8659`

## 做了什么

- 复盘用户提供的目标图、Round 14 最终截图、视觉规格、实现计划和当前工作台代码。
- 确认 Round 14 的主要缺口不是功能未完成，而是中心画布占比、纹理可见度、无预览卡片、表面层级和通用后台盒子感未达到目标图。
- 比较字面 A、受保护 A 和分阶段 A 三种路径。
- 用户确认采用“受保护的 A”：尽量还原美术构图和材质，同时冻结产品、数据和核心交互合同。
- 新增书面设计规格 `docs/superpowers/specs/2026-08-01-protected-high-fidelity-workstation-design.md`。

## 为什么做

Round 14 已通过功能和工程门禁，但视觉还原主要停留在文字、控件和信息顺序。Round 15 需要把高保真目标转化为可量化验收约束，同时避免为了照抄概念图推翻已经验证的业务能力。

## 影响范围

- 当前仅新增和更新设计、状态与时间线文档，没有修改运行时代码。
- 计划中的实现重点为工作台构图、背景、参考卡、工具栏、左侧研究栏、右侧检查器、对比坞、原创分类纹理和只读雷达图。
- 明确不修改 API、D1、migration、领域模型、Backup v1、来源策略和生产业务数据。

## 验证结果

- 核对当前 `main` 与 `origin/main` 同步，设计开始前工作树干净。
- 核对 `workspace_layout.version: 1` 同时进入 Backup v1，确认本轮不能通过简单版本升级重置布局。
- 核对左侧筛选当前为单选状态，确认高保真重做不引入筛选多选。
- 核对 comparison 已有显式模式与 2-4 条有序选择，确认本轮只强化可发现性和反馈。
- 书面规格仍待用户复核，尚未进入代码实现和完整工程门禁。

## 风险或遗留

- 分类纹理需要在原创性、可读性和传输体积之间平衡。
- 新默认布局需要一次性迁移，但必须保持 Backup v1 的布局版本兼容。
- 视觉验收必须使用多类别密集 fixture，不能只依赖生产两条 seed reference。
- 实现计划尚未编写；设计规格批准前不修改应用源码。

## 下一步

- 用户已批准书面设计规格。
- 使用 `superpowers:writing-plans` 写入 `docs/superpowers/plans/2026-08-01-protected-high-fidelity-workstation.md`。
- 计划拆分为中心优先布局迁移、原创分类纹理与共享预览、视觉基底、命令轨道与参考卡、评分雷达与检查器、有序对比反馈、次级流程与响应式、完整本地 QA、独立复审、合并部署十项。
- 计划自检发现 `224/360` 默认栏宽在 1480px 无法稳定满足中心至少 60%，因此在批准范围内校正为 `220/352`，对应中心 `892px / 60.27%`。
- 当前等待用户选择 Subagent-Driven 或 Inline Execution；选择即视为实施计划批准和执行授权。

## 实施启动

- 用户选择 Subagent-Driven，实施计划获批并获得执行授权。
- 将仓库根目录复位到同步 `main`，把 `codex/round-15-protected-a` 挂载到 `.worktrees/round-15-protected-a`，所有 Round 15 实现仅在隔离 worktree 内进行。
- 按 SDD 计划建立专属 `.superpowers/sdd/2026-08-01-protected-high-fidelity-workstation/` ledger；Windows linked-worktree 的 `.git` 路径无法由 WSL Git 解析，因此按技能脚本同等逻辑创建目录与忽略规则。
- 隔离 worktree 执行 `npm install` 成功；完整基线为 39 个测试文件 / 422 项测试通过。
- `npm install` 报告 19 项依赖审计告警（1 low、4 moderate、13 high、1 critical）；本轮未改变依赖范围，也未执行可能引入破坏性升级的自动修复。

## Delegation Log

- 设计与计划阶段由主代理单线完成。
- 实施采用 Subagent-Driven：每项任务由新 implementer 执行并提交，随后由独立 task reviewer 完成规格符合性与代码质量双重检查；主代理负责 ledger、冲突裁决、任务衔接和最终整体验证。
- Task 1 implementer：实现中心优先默认值、边界、旧偏好迁移、hook/page 接线和 Backup v1 回归；提交 `c41be49`，focused 3 文件 / 99 项测试与 typecheck 通过。
- Task 1 reviewer：只读检查 brief、report 与 `21fcabc..c41be49` 完整 diff；规格与质量均 `Approved`，Critical/Important/Minor 均为 0。

## Task 1 结果

- 新默认栏宽为 `220/352`，范围为 `208-320/336-520`，中心最小宽度为 `640`。
- 新 localStorage key 为 `ref-forge-workspace-layout-r15-v1`；旧 `ref-forge-workspace-layout-v1` 只参与一次迁移。
- 精确旧默认 `260/420` 重置为新默认并保留折叠状态；自定义旧宽度按新边界保留和收敛。
- `workspace_layout.version` 与 Backup v1 schema 继续为 `1`。
- 1480px 默认布局解算中心为 `892px`，占比约 `60.27%`。

## Task 2 结果

- 提交 `57e356c` 新增 8 个资产分类与 generic 回退的原创 `1600x900` SVG，实际总体积 `15,660 bytes`，单张最大 `1,926 bytes`。
- 新增共享 `ReferencePreview`；本地分类纹理始终作为底层，远程预览成功时覆盖，失败时回退，同一记录 URL 改变后会重试。
- ReferenceCard 与 ComparisonDock 复用同一预览实现，原有选择、置顶、复选语义、移除与折叠行为保持。
- focused 5 文件 / 20 项测试、typecheck、完整 41 文件 / 434 项测试通过；9 张 SVG 经 Edge headless 成功栅格化并逐张抽查。
- 独立审查规格 `PASS`、质量 `APPROVED WITH MINOR`；Critical/Important 为 0。唯一 Minor 是实现报告将总字节数少写 1 byte，不影响资产预算或代码。

## Delegation Log 补充

- Task 2 implementer：实现原创分类纹理、映射、共享预览及 card/dock 接入；提交 `57e356c`，focused、typecheck 和完整测试通过。
- Task 2 reviewer：只读审查 `5af035f..57e356c` 完整 diff 并独立栅格化 SVG；无阻塞 finding，记录 1 项报告精度 Minor。

## Task 3 结果

- 提交 `997b2e1` 建立 protected-A 石墨/表面/边界/信号/文字 token，body 使用静态径向暗光与既有 WebP 组合。
- 左、中、右追加纯展示类 `research-rail`、`reference-canvas`、`reference-inspector`，保持 DOM、状态、单选筛选、视图切换与 splitter 合同。
- 中央画布表面降至 `rgba(9, 13, 15, 0.38)`；rail/inspector 转为连续表面，详情段落主要通过横向分隔和明度组织。
- focused 3 文件 / 19 项、完整 42 文件 / 439 项、typecheck、lint、diff check 与 Impeccable detector 通过。
- 独立审查规格 `PASS`、质量 `APPROVED WITH MINOR`，Critical/Important 为 0。Minor 为 `--surface-canvas` 与 gallery literal 重复、合同正则作用域过宽；已记入 ledger 并交给 Task 4 CSS/测试范围收口。
- live 同视口视觉比较仍是后续集成 QA 边界，不作为本 Task 缺陷。

## Delegation Log Task 3

- implementer：完成三栏视觉基底、响应式覆盖与静态合同，提交 `997b2e1`。
- reviewer：只读审查 `b3de10e..997b2e1`，给出 19/20 task-scoped audit，0 个阻塞 finding。

## Task 4 结果

- 提交 `a9d79d9` 将参考工具栏整合为单一命令轨道，并让搜索成为最大弹性轨道；所有原 props、回调和 disabled 状态保持。
- 参考卡继续复用 ReferencePreview，强化 16:9 图像层并移除大面积分类纯色；紧凑/舒适网格使用 `auto-fit minmax(214px/282px, 1fr)` 与 `10/14px` 间距。
- 收口 Task 3 Minor：gallery 改为消费 `--surface-canvas`，本轮触及的 CSS 合同改为准确 rule-scope。
- 首轮审查发现移动命令轨道 button/input/select 仍可能为 40px，判为 1 项 Important；原 implementer 在 fix round 1 先补红灯，再于提交 `21f8994` 将三类控件最小高度设为 44px。
- fix covering tests 为 3 文件 / 13 项，typecheck、lint 通过；scoped re-review 判定 finding `ADDRESSED`，无新 Critical/Important。
- Task 4 完整测试基线为 42 文件 / 440 项通过。

## Delegation Log Task 4

- implementer：完成命令轨道、图像型卡片、网格与 token/test Minor 收口；提交 `a9d79d9`。
- reviewer：指出移动命令控件触控高度 Important。
- implementer fix round 1：提交 `21f8994`，补精确回归和 44px 规则。
- scoped re-reviewer：只审 `a9d79d9..21f8994`，确认原 finding 已解决且无新 breakage。

## Task 5 结果

- 提交 `12c787f` 新增五轴评分画像 helper 与无依赖 SVG 雷达图，轴顺序为评分、参考价值、可转化性、制作就绪度、安全性；安全性严格使用 `6 - copyright risk`，未替换权威数字矩阵中的原始风险值。
- 缺失或无效评分显示本地化不完整提示且不输出数据多边形；完整图形保留可访问名称、五层网格、轴线、标签和值。
- 详情区顺序、固定来源与安全区、disclosure ARIA、质量补全导航和删除回调保持不变；完整验证通过 44 个测试文件 / 449 项、typecheck、lint 与 detector。
- 首轮审查发现旧 `.detail-panel section` 的 specificity 使详情行仍计算为独立卡片；fix round 1 提交 `e4b1a59`，用 `.detail-panel .reference-detail > :where(.detail-section)` 显式重置 border、radius、background、padding 和 gap，并让相邻 border-top 成为唯一行间分隔。
- 修复先补失败合同，再通过 2 文件 / 14 项覆盖测试、typecheck、lint、diff check；scoped re-review 判定原 Important `ADDRESSED`，无新 Critical/Important。

## Delegation Log Task 5

- implementer：完成评分画像、雷达图、连续详情结构与回归测试，提交 `12c787f`。
- reviewer：只读审查 `933aa2b..12c787f`，确认评分、SVG/a11y、本地化和行为合同，指出连续表面 CSS 级联 Important。
- implementer fix round 1：提交 `e4b1a59`，补能捕获 specificity 失效的红测并修复定向重置规则。
- scoped re-reviewer：只审 `12c787f..e4b1a59`，确认 fixed/collapsible section 均被压平、相邻分隔线继续胜出，且无新 breakage。

## Task 6 结果

- 提交 `66c0b3b` 新增 `comparisonPosition`，直接从既有 `comparisonReferenceIds` 的插入顺序派生 1-based 位置，没有新增选择 store、拖拽、重排或键盘修饰路径。
- 已选对比卡显示实心数字方块，未选卡显示 plus；对比坞持续显示 `{count} / 4`、双语顺序提示和低于 2 条时的明确缺口数量。
- 保持最多 4 条、至少 2 条交接、正常单击、取消、Escape 折叠、综合稿 handoff、有序列表、移除按钮、`role=status`、`aria-live` 与对比模式 pin disabled。
- focused 5 文件 / 30 项、完整 44 文件 / 450 项、typecheck、lint 与 diff check 通过。
- 独立审查规格 `PASS`，Critical/Important 为 0；唯一 Minor 是缺少页面级 ordered IDs 到多张卡片 1-based 编号的自动化联动，已明确带入 Task 8 浏览器 QA，并覆盖未选 plus 与上限 disabled/not-allowed。

## Delegation Log Task 6

- implementer：测试先行完成编号标记、持续提示、状态样式与原状态机接线，提交 `66c0b3b`。
- reviewer：只读审查 `62b83c6..66c0b3b`，确认规格与质量通过，无阻塞 finding，记录 1 项后续浏览器联动覆盖 Minor。
