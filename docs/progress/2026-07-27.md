# 2026-07-27

## Round 13 全库备份与受控恢复设计

- 做了什么：按 Superpowers brainstorming 完成 Round 13 需求澄清、三种架构比较和四段设计确认，并写入 `docs/superpowers/specs/2026-07-27-full-library-backup-restore-design.md`。
- 为什么做：当前 Round 8 全库 JSON 只包含 references，无法恢复 Round 11 后新增的 syntheses、relation 顺序和历史 snapshot；项目需要可移植、可预览且不会整库删除的真实恢复能力。
- 已确认范围：受控恢复、可选设备偏好、仅支持 Backup v1、透明 JSON、领域级 API、生产临时 QA 数据完整恢复测试。
- 旧格式检查：在项目、桌面和下载目录只检测到 `C:\Users\彭小平\Downloads\ref-forge-library-2026-06-29.json`；它仅包含两个 seed 示例 ID，不需要兼容。文件未删除。
- 影响：本次只新增和更新设计/进度文档，未修改应用代码、API、D1、migration 或生产数据；Sites version 14 继续作为稳定生产基线。
- 验证：规格占位符扫描无未决项；自审补充每稿 `2-4` 条连续 relation、非空 relation/snapshot ID 一致、`409 backup_changed`，并把设备布局字段纠正为复用现有 `version: 1` 契约。实现计划前复核当前 D1 官方限制，确认 batch 事务回滚语义及 Free 计划 50 查询、100 bindings、100 KB SQL、2 MB 字符串限制，规格改为小于 1 MB 的 JSON1 分块和最多 40 条 batch 语句；`git diff --check` 无空白错误，文档路径和阶段引用一致。
- 风险或遗留：书面规格仍需用户复核；实现计划尚未编写；无全局数据库 revision 时 state digest 与 D1 batch 间存在极窄并发窗口，当前以 owner-only 单用户边界接受。
- 下一步建议：用户复核并批准书面规格后，使用 `superpowers:writing-plans` 编写 TDD 实现计划。

## Round 13 规格批准与实现计划

- 做了什么：用户批准书面规格并要求后续执行直到完成；按 `superpowers:writing-plans` 写入 `docs/superpowers/plans/2026-07-27-full-library-backup-restore.md`，拆分格式合同、导出预览、原子恢复、API、数据管理对话框、工作台集成和交付收口七项。
- 为什么做：把已批准设计转成可逐项 TDD、独立审查和精确验证的执行合同，避免直接在 `main` 修改应用。
- 影响：更新规格状态、计划、AGENTS 和三份留痕；应用、API、D1、migration、依赖和生产数据仍未变化。
- 技术复核：Cloudflare D1 `batch()` 具备事务回滚语义，但 Free 计划每 invocation 50 查询；计划采用原生 D1 prepared statements、JSON1 分块、单块小于 1 MB、batch 不超过 40 条。
- 执行模式：用户已要求持续执行，采用推荐的 Subagent-Driven Development；主代理负责分支/worktree、任务集成、审查、完整门禁、Sites 和生产收口。
- 计划自审：逐节对照规格后无覆盖缺口；模糊占位符扫描无匹配；`parseRefForgeBackup`、digest、preview、restore、dialog、workspace preference 和 restore epoch 接口跨任务一致；`git diff --check` 无空白错误。
- 下一步建议：自审并提交/推送设计计划基线，从同步 `main` 创建 `codex/round-13-backup-restore` 隔离 worktree，运行基线测试后执行 Task 1。

## Round 13 实现与本地验收

- 做了什么：在 `codex/round-13-backup-restore` 隔离 worktree 完成 Backup v1 严格格式、全库导出与零写入预览、原生 D1 batch 原子恢复、三条 backup API、双语数据管理对话框和 reference/synthesis 工作台恢复协调。
- 为什么做：原 reference-only JSON 无法保护 Round 11 后的 synthesis、ordered relation 和历史 snapshot，也没有恢复预览、并发门禁或设备偏好选择。
- 影响：新增 `lucide-react` 和 `/api/backup`、`/api/backup/preview`、`/api/backup/restore`；不新增 migration，不删除备份未包含的数据，不改变来源与版权策略。
- TDD 与修复：逐任务覆盖旧格式拒绝、closed schema、完整 state digest、JSON1 分块、40 语句上限、SQLite 回滚、稳定错误、异步 single-flight、草稿保护和偏好求交集。本地浏览器另发现并修复 D1 1-2 位小数时间戳回导失败、stale 自动重预览提示丢失和 390px 按钮图标错位。
- 验证：31 个测试文件 / 390 项测试、typecheck、lint、build、`git diff --check` 通过；真实 SQLite 注入末尾无效 relation 后四表完全回滚；验收 fixture 使用 4 条 batch 语句，最大 JSON1 参数块 2,386 字节。
- 浏览器：恢复 2 references、1 synthesis、2 ordered relations，精确验证 null-source snapshot；stale 409、reference/synthesis 未保存草稿门禁、中英文、Escape/焦点、1600/1280/390px 横向溢出 0、console error 0 均通过；最终 API 回读 references 0、syntheses 0。
- 审查：Tasks 1-5 使用重要 subagent 进行实现/规格/质量复核并形成修复循环；Task 6 后 subagent 额度耗尽，主代理按最终威胁清单复核全分支，未发现未解决 Critical/Important。该项不是独立审查，merged-main 全门禁仍必须执行。
- 风险或遗留：owner-only 单用户边界下接受 state digest 复核与 D1 batch 之间的极窄并发窗口；待完成 GitHub 合并同步、Sites 新版本部署和认证生产临时数据恢复闭环。
- 下一步建议：提交本地证据，fast-forward 合并 `main`，重新运行完整门禁并部署精确提交。

## Delegation Log

- 用途：Tasks 1-5 的实现、规格符合性和代码质量复核。
- 范围：Backup v1 parser/digest、全库 inventory/preview、D1 restore、API request boundary、数据管理状态与异步交互。
- 模式：Subagent-Driven Development，任务级 implementer 与 reviewer 循环；主代理审阅 diff、运行 focused/full gates 并整合修复。
- 结果：修复深度遍历、全库 state digest、批量读取、零操作 batch、枚举校验、请求上限、异步 race、single-flight 和取消门禁等问题。
- 收口：Task 6 开始后临时 subagent 额度耗尽；未再派发新 agent，主代理接管工作台接线、本地浏览器验收和最终全分支复核。最终文档明确保留该独立审查证据边界。
